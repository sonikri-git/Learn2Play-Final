import React,{useState} from 'react';import {Link,useNavigate} from 'react-router-dom';import {api} from '../api';import AuthScene from '../components/AuthScene';import '../styles/signup.scss';
export default function SignupPage(){const nav=useNavigate();const [form,setForm]=useState({name:'',email:'',password:''});const [msg,setMsg]=useState('');const submit=async e=>{e.preventDefault();try{await api.post('/auth/register',form);alert('Account created successfully');nav('/login')}catch(err){setMsg(typeof err.response?.data==='string'?err.response.data:'Failed to create account')}};return <div className="signup-page"><AuthScene variant="signup"/><main className="auth-panel"><div className="auth-box rise-in"><nav className="tabs"><Link to="/signup" className="tab active">Sign up</Link><Link to="/login" className="tab">Log in</Link></nav><h2>Create your account</h2><p className="subtitle">Join Learn2Play — it's free.</p><form onSubmit={submit}>{[['name','Full name','Your name','text'],['email','Email','you@example.com','email'],['password','Password','At least 6 characters','password']].map(([k,l,p,t])=><div className="field-group" key={k}><label>{l}</label><div className="field"><input type={t} placeholder={p} value={form[k]} onChange={e=>setForm({...form,[k]:e.target.value})} required/></div></div>)}{msg&&<p className="form-error">{msg}</p>}<button className="btn-primary" type="submit">Create account</button></form><p className="legal">By continuing, you accept Learn2Play's <a href="#">Terms of Service</a> and <a href="#">Privacy Policy</a>.</p><div className="card-footer"><span>Already have an account?</span><Link to="/login">Sign in</Link></div></div></main></div>}
