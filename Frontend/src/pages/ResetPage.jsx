import React,{useState} from 'react';import {Link,useNavigate,useSearchParams} from 'react-router-dom';import {api} from '../api';import AuthScene from '../components/AuthScene';import '../styles/reset.scss';
export default function ResetPage(){const [p,setP]=useState('');const [c,setC]=useState('');const [msg,setMsg]=useState('');const [params]=useSearchParams();const nav=useNavigate();const submit=async e=>{e.preventDefault();if(p.length<6)return setMsg('Password must be at least 6 characters');if(p!==c)return setMsg('Passwords do not match');try{await api.post('/auth/reset-password',{token:params.get('token')||'',password:p});alert('Password updated successfully');nav('/login')}catch(err){setMsg(typeof err.response?.data==='string'?err.response.data:'Unable to reset password')}};return <div className="reset-page"><AuthScene variant="reset"/><main className="auth-panel"><div className="auth-box rise-in"><Link to="/login" className="back-link"><svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round"><path d="M19 12H5M12 19l-7-7 7-7"/></svg>Back to login</Link><h2>Reset your password</h2><p className="subtitle">Enter your new password below.</p><form onSubmit={submit}><div className="field-group"><label>New password</label><div className="field"><input type="password" value={p} onChange={e=>setP(e.target.value)} placeholder="At least 6 characters" required/></div></div><div className="field-group"><label>Confirm password</label><div className="field"><input type="password" value={c} onChange={e=>setC(e.target.value)} placeholder="Re-enter your password" required/></div></div>{msg&&<p className="form-error">{msg}</p>}<button className="btn-primary">Reset password</button></form><div className="card-footer"><span>Remembered it after all?</span><Link to="/login">Log in</Link></div></div></main></div>}
