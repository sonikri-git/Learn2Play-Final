import React,{useState} from 'react';import {Link,useNavigate} from 'react-router-dom';import {api} from '../api';import AuthScene from '../components/AuthScene';import '../styles/forgot.scss';
export default function ForgotPage(){const [email,setEmail]=useState('');const [loading,setLoading]=useState(false);const [msg,setMsg]=useState('');const nav=useNavigate();const submit=async e=>{e.preventDefault();setLoading(true);try{await api.post('/auth/forgot-password',{email});setMsg('Password reset email sent.');setTimeout(()=>nav('/login'),1200)}catch(err){setMsg(typeof err.response?.data==='string'?err.response.data:'Unable to send reset email')}finally{setLoading(false)}};return <div className="forgot-page"><AuthScene variant="forgot"/><main className="auth-panel"><div className="auth-box rise-in"><Link to="/login" className="back-link"><svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round"><path d="M19 12H5M12 19l-7-7 7-7"/></svg>Back to login</Link><h2>Forgot your password?</h2><p className="subtitle">No worries — enter your email and we'll send a reset link.</p><form onSubmit={submit}><div className="field-group"><label>Email address</label><div className="field"><input type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="you@example.com" required/></div></div>{msg&&<p className="form-error">{msg}</p>}<button className="btn-primary" disabled={loading}>{loading?'Sending…':'Send reset link'}</button></form><div className="card-footer"><span>Remembered it after all?</span><Link to="/login">Log in</Link></div></div></main></div>}
