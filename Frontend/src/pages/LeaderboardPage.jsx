import React,{useEffect,useState} from 'react';import {useNavigate} from 'react-router-dom';import Navbar from '../components/Navbar';import {api} from '../api';import '../styles/leaderboard.scss';
export default function LeaderboardPage(){const [list,setList]=useState([]),[loading,setLoading]=useState(true);const nav=useNavigate();useEffect(()=>{api.get('/leaderboard').then(r=>setList(r.data||[])).catch(()=>{}).finally(()=>setLoading(false))},[]);return <><Navbar/><div className="leaderboard-page"><div className="container"><div className="header"><h1>🏆 Leaderboard</h1><p>Top Performing Learners</p></div>{loading?<div className="loading">Loading Leaderboard...</div>:<div className="cards">{list.map(u=><div className={'user-card '+(u.rank===1?'gold':u.rank===2?'silver':u.rank===3?'bronze':'')} key={u.email}><div className="left"><div className="avatar">{(u.name||'?')[0]}</div></div><div className="middle"><div className="name">{u.name}</div><div className="email">{u.email}</div><div className="stats"><div>⭐ Average <strong>{Number(u.averageScore||0).toFixed(2)}%</strong></div><div>🏆 Highest <strong>{Number(u.highestScore||0).toFixed(2)}%</strong></div><div>📚 Quizzes <strong>{u.quizzesTaken}</strong></div></div></div><div className="right"><div className="rank">{u.rank===1?'👑 ':u.rank===2?'🥈 ':u.rank===3?'🥉 ':''}#{u.rank}</div></div></div>)}</div>}<div className="buttons"><button onClick={()=>nav('/dashboard')}>Dashboard</button><button onClick={()=>nav('/history')}>Quiz History</button></div></div></div></>}
